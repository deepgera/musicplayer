{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js " ,
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd webpage && npm install && npm run build",
    "heroku-postbuild":"cd webpage && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "v14.16.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "path": "^0.12.7",
    "ytdl-core": "^4.9.1"
  }
}
